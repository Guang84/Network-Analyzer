/*window.addEventListener('DOMContentLoaded', () => {
    const csvFrame = document.getElementById('');
    const csvUrl = '/home/hope/Desktop/Final/SaveData-01.csv'; // Replace this with your CSV file URL

    // Set the src attribute of the iframe to the CSV file URL
    csvFrame.src = csvUrl;
}); */